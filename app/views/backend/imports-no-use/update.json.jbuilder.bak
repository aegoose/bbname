if @isError
  json.code 1
  json.message "更新失败"
  json.data []
else
  json.code 0
  json.message ""
  json.data do
    json.id @admin_log.id
    json.url process_customer_backend_import_path(@admin_log)
    # json.partial! "admin_log", admin_log: @admin_log
  end
end
